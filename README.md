# I2C_implementation_VHDL
A minimal and readable implementation of a I2C module in VHDL

Developed in Vivado, not the best code practices, mainly for exploration pourposes. May update later is required.
